# 四个邮箱模块共用一个邮箱入口

邮箱助手保留一个简单前台入口，但将 Gmail、Yahoo、GMX 和 Outlook 作为不可变的持久化邮箱服务商身份，分别路由到独立邮箱模块。选择这种结构是为了让服务商授权、收件、发件和状态判断集中在各自模块中，同时避免用户接触协议差异；运行时不得再依赖邮箱后缀决定收发实现。

## Consequences

邮箱后缀只允许用于新建、导入和旧数据迁移时识别服务商。账号创建后不能在编辑页切换服务商；共享 IMAP、SMTP、归档实现不得包含 Gmail、Yahoo、GMX、Outlook 条件分支。
