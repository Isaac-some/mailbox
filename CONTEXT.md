# 邮箱助手

邮箱助手集中管理邮箱账号，并以统一、直接的操作入口完成收件同步、邮件归档和发件。

## Language

**邮箱服务商**：
实际负责账号收发能力的一方，当前只包括 Gmail、Yahoo、GMX 和 Outlook。邮箱服务商是账号创建后不可改变的持久化身份。
_Avoid_: IMAP 类型、MSA 类型、邮箱后缀类型

**邮箱模块**：
独占一个邮箱服务商的账号准备、授权、收件、发件和能力判断规则。四个邮箱模块共享归档等通用实现，但不共享服务商条件判断。
_Avoid_: 邮箱分支、服务商判断

**邮箱入口**：
用户添加、查看、同步和发件的统一前台入口。邮箱入口不要求用户理解 IMAP、SMTP、OAuth 或后台模块结构。
_Avoid_: 服务商入口、协议入口

**收发能力**：
邮箱账号当前是否可以收件、发件，以及需要用户执行什么动作才能恢复。前台状态和实际执行必须来自同一个邮箱模块判断。
_Avoid_: MSA 状态、CanSend 特例
